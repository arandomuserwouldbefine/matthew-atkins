import { Kalnia, Work_Sans } from "next/font/google";

export const work_sans = Work_Sans({ subsets: ["latin"], variable: "--font-workSans" });

export const kalnia = Kalnia({ subsets: ["latin"], variable: "--font-kalnia" });
